{
  "name": "futures",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pm2 stop all && pm2 delete all && pm2 start pm2.json --only dev && pm2 logs",
    "start": "pm2 stop all && pm2 delete all && pm2 start pm2.json --only prod"
  },
  "author": "neo",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "exceljs": "^4.3.0",
    "express": "^4.18.2",
    "express-fileupload": "^1.4.0",
    "moment": "^2.29.4",
    "mongodb": "^6.1.0",
    "morgan": "^1.10.0"
  }
}
